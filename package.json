{
  "name": "x402charity-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "start": "pnpm --filter @x402charity/server start",
    "dev": "pnpm -r build && node --env-file=.env packages/server/bin/server.js",
    "clean": "pnpm -r clean",
    "vercel-build": "pnpm --filter x402charity build && pnpm --filter @x402charity/server build && mkdir -p public/assets public/fonts && cp docs/index.html public/index.html && cp docs/colors_and_type.css public/colors_and_type.css && cp docs/x402-charity.js public/x402-charity.js && cp -R docs/assets/. public/assets/ && cp -R docs/fonts/. public/fonts/"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
